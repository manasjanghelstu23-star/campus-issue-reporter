// Toggle this to false when connecting to a real backend API
export const USE_MOCK_API = false;

// Backend developer can change this string (10.0.2.2 maps to computer localhost on Android emulator)
export const BASE_URL = 'http://10.0.2.2:5000/api';
